{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -q '^git push' && { xcrun agvtool next-version -all && git commit -m 'Bump build number' -- iPhoneSpinner5.xcodeproj/project.pbxproj iPhoneSpinner5/Info.plist ; } 2>/dev/null || true",
            "statusMessage": "Incrémentation du numéro de build…"
          }
        ]
      }
    ]
  }
}
